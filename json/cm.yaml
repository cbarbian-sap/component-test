apiVersion: v1
kind: ConfigMap
metadata:
  name: echo
data:
  values: |-
    {{- component.Spec.Values.Raw | toString | nindent 4 }}
  valuesJson: |-
    {{- component.Spec.Values | toJson | nindent 4 }}
