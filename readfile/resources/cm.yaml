---
apiVersion: v1
kind: ConfigMap
metadata:
  name:  a
data:
  foo1: "{{ (readFile "../versions.yaml" | toString | fromYaml).foo }}"
  foo2: "{{ (readFile "../../versions.yaml" | toString | fromYaml).foo }}"
  foo2: "{{ (readFile "../../../versions.yaml" | toString | fromYaml).foo }}"
