---
apiVersion: core.cs.sap.com/v1alpha1
kind: Component
metadata:
  namespace: default
  name: "z"
spec:
  requeueInterval: 1m
  reapplyInterval: 1m
  sourceRef:
    fluxGitRepository:
      namespace: default
      name: component-test
  path: "z"
  values:
    appName: myapp
    hanaInstanceId: hid-only
    aicoreModelName: gpt-5
    
